import React from 'react'

function DisectingProps() {
    return React.createElement(
        "div", null, React.createElement(
            "h2", null, "Disecting Props"
        )

    )
}

export default DisectingProps